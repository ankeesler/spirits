---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: spirits.spirits.ankeesler.github.io
spec:
  group: spirits.ankeesler.github.io
  names:
    categories:
    - spiritsworld
    kind: Spirit
    listKind: SpiritList
    plural: spirits
    singular: spirit
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.attributes.stats.health
      name: Health
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Spirit is the Schema for the spirits API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: SpiritSpec defines the desired state of Spirit
            properties:
              action:
                description: Action contains the description of the Action this Spirit
                  performs
                maxProperties: 1
                minProperties: 1
                properties:
                  choices:
                    description: Choices specify a list of Action's a Spirit could
                      potentially perform
                    properties:
                      actions:
                        description: Actions are the SpiritAction's from which the
                          Spirit can chose
                        items:
                          description: NamedSpiritAction is a type holding an Action
                            and a unique name used to reference that Action
                          properties:
                            action:
                              description: Action is the SpiritAction describing this
                                Action
                              x-kubernetes-preserve-unknown-fields: true
                            name:
                              description: Name is a unique name used to reference
                                this Action
                              type: string
                          required:
                          - action
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      intelligence:
                        description: Intelligence holds the strategy via which one
                          of the below Action's will be chosen
                        type: string
                    required:
                    - actions
                    - intelligence
                    type: object
                  registry:
                    description: Registry holds the HTTP information to GET an Action
                    properties:
                      certificateAuthorityData:
                        description: CertificateAuthorityData holds the optional CA
                          bundle data used to validate TLS connections to the URL
                        format: byte
                        type: string
                      url:
                        description: URL holds the location of the HTTP endpoint;
                          the endpoint must use HTTPS
                        minLength: 1
                        pattern: ^https://
                        type: string
                    required:
                    - url
                    type: object
                  script:
                    description: Script holds the source of a script used to implement
                      a Spirit's Action
                    properties:
                      text:
                        description: Text holds the text of the script
                        type: string
                    required:
                    - text
                    type: object
                  wellKnown:
                    description: WellKnown is a string identifying a well-known (builtin)
                      Action
                    enum:
                    - Attack
                    - Noop
                    type: string
                type: object
              attributes:
                description: Attributes describe the in-battle spirit's qualities
                properties:
                  stats:
                    description: Stats are the current statistics that describe this
                      Spirit
                    properties:
                      agility:
                        description: Agility is a quantitative representation of the
                          speed of the Spirit.
                        format: int64
                        minimum: 0
                        type: integer
                      armor:
                        description: Armor is a quantitative representation of the
                          defending ability of the Spirit.
                        format: int64
                        minimum: 0
                        type: integer
                      health:
                        description: Health is a quantitative representation of the
                          energy of the Spirit. When this drops to 0, the Spirit is
                          no longer able to participate in a Battle.
                        format: int64
                        minimum: 0
                        type: integer
                      power:
                        description: Power is a quantitative representation of the
                          attacking ability of the Spirit.
                        format: int64
                        minimum: 0
                        type: integer
                    type: object
                type: object
            required:
            - action
            type: object
          status:
            description: SpiritStatus defines the observed state of Spirit
            properties:
              conditions:
                description: Conditions represents the observations of a Spirit's
                  current in-API state
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource. --- This struct is intended for direct
                    use as an array at the field path .status.conditions.  For example,
                    type FooStatus struct{ // Represents the observations of a foo's
                    current state. // Known .status.conditions.type are: \"Available\",
                    \"Progressing\", and \"Degraded\" // +patchMergeKey=type // +patchStrategy=merge
                    // +listType=map // +listMapKey=type Conditions []metav1.Condition
                    `json:\"conditions,omitempty\" patchStrategy:\"merge\" patchMergeKey:\"type\"
                    protobuf:\"bytes,1,rep,name=conditions\"` \n // other fields }"
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition
                        transitioned from one status to another. This should be when
                        the underlying condition changed.  If that is not known, then
                        using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating
                        details about the transition. This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation
                        that the condition was set based upon. For instance, if .metadata.generation
                        is currently 12, but the .status.conditions[x].observedGeneration
                        is 9, the condition is out of date with respect to the current
                        state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating
                        the reason for the condition's last transition. Producers
                        of specific condition types may define expected values and
                        meanings for this field, and whether the values are considered
                        a guaranteed API. The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        --- Many .condition.type values are consistent across resources
                        like Available, but because arbitrary conditions can be useful
                        (see .node.status.conditions), the ability to deconflict is
                        important. The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              phase:
                default: Pending
                description: Phase summarizes the overall status of the Spirit API
                  object
                enum:
                - Pending
                - Ready
                - Error
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
